import React from 'react';
import {View, Text} from 'react-native';
import {TheText} from '../TheText';
import {TheButton} from '../TheButton';

export const PdfViewer = () => {
  return (
    <View>
      <TheButton title="Atras" styles={{width: 50}} />
    </View>
  );
};
